export const isAdult = (age) => {
	return age > 18 ? true : false 
}

const canDrink = (age) =>{
	return age > 18 ? true : false 
}

const isSenior = (age) => age > 68 ;
export default isSenior ;

export {canDrink};